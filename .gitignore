# secrets / local config — never commit
# keys.json is intentionally committed (baked into the image, private repo)
*.bak

# local temp artifacts
_*.txt
_uitest/
_uitest.log
node_modules/
.git/
*.log
